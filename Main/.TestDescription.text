📈 สิ่งที่โค้ดนี้จะวัดได้
✅ ตรวจว่า server รับ client พร้อมกันหลายตัวได้ไหม
✅ วัด “เวลาเฉลี่ยต่อข้อความ” (latency)
✅ วัด “throughput” (จำนวนข้อความต่อวินาที)
✅ ตรวจความเสถียรของระบบ concurrency
✅ ใช้ได้เพื่อเปรียบเทียบตอนเปลี่ยนจำนวน thread ของ BroadcasterPool

1️⃣ Total Time (s)
คือ เวลาที่ใช้ทั้งหมดในการส่งข้อความทั้งหมดของทุก client
ในกรณีนี้ ทุกจำนวน threads ใช้เวลาประมาณ 25.7–25.8 วินาที
บอกว่า: การเพิ่มจำนวน threads ไม่ช่วยให้ส่งข้อความเร็วขึ้น เพราะ server/network/client เป็น bottleneck ที่ไม่ใช่จำนวน threads

2️⃣ Avg Latency (ms/msg)
คือ เวลาที่ใช้ต่อ message (ตั้งแต่ client ส่ง → ได้รับ echo กลับ)
หน่วยเป็น milliseconds ต่อ message
ในกรณีนี้ประมาณ 513–514 ms/message
บอกว่า: การเปลี่ยนจำนวน threads ไม่ลด latency เลย → latency ถูกจำกัดโดย factor อื่น เช่น server processing, network delay, socket timeout หรือ sleep loop ในโค้ด client

3️⃣ Throughput (msg/s)
คือ จำนวนข้อความต่อวินาที
คำนวณจาก: Throughput = จำนวนทั้งหมดของ message / Total Time (s)
ตัวอย่าง: 50 clients × 50 messages = 2500 messages
Throughput = 2500 / 25.78 ≈ 97 (ตัวเลขของคุณเป็น 3.88 อาจเพราะหน่วยหรือ calculation มีการ normalize)
บอกว่า: server ไม่สามารถส่ง message ได้เร็วกว่านี้ → threads เพิ่มไม่ช่วย throughput

🔹 สรุปภาพรวมจากตารางนี้
1.Threads เพิ่ม → ไม่มีผลต่อ Total Time / Latency / Throughput
→ server หรือ network เป็น bottleneck ที่แท้จริง
2.ค่า latency ค่อนข้างสูง (~500ms/message)
→ อาจมาจาก socket.setSoTimeout(1000) และ safeRead() loop ใน client
3.Throughput ต่ำมาก (3–4 msg/s)
→ สะท้อนว่า client-server communication มี delay สูง หรือ test configuration ส่ง message ช้า